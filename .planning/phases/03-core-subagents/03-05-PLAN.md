---
phase: 03-core-subagents
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified: []
autonomous: false
requirements:
  - SECU-01
  - SECU-05
  - MVCA-01
  - MVCA-04
  - STAT-01
  - STAT-04

must_haves:
  truths:
    - "All three subagents appear in the launcher sidebar with correct icons"
    - "Clicking a parameterized subagent shows an inline input form"
    - "Submitting the form triggers the subagent and results appear in the chat timeline"
    - "Results contain structured markdown with SQL code blocks"
  artifacts: []
  key_links: []
---

<objective>
Visual verification that all three Phase 3 subagents (Security Audit, MVC Analysis, Statistics Analysis) are working end-to-end: visible in launcher, parameter forms render, and a test run produces structured output in the result card.

Purpose: Confirms the full pipeline works before marking the phase complete — from launcher click through parameter form to IPC to Bedrock API to tool-use loop to result card rendering.
Output: User-verified confirmation that subagents are functional.
</objective>

<execution_context>
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-subagents/03-01-SUMMARY.md
@.planning/phases/03-core-subagents/03-02-SUMMARY.md
@.planning/phases/03-core-subagents/03-03-SUMMARY.md
@.planning/phases/03-core-subagents/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch the application for verification</name>
  <files></files>
  <read_first>
    - package.json (build scripts)
  </read_first>
  <action>
Run the development build:
```bash
cd /Users/Daniel.Tehan/Code/dba && npm run dev
```

Wait for the app to launch. If there are build errors, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with zero errors
    - Dev server starts without crash
  </acceptance_criteria>
  <done>Application builds and launches successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all three subagents in the UI</name>
  <files></files>
  <action>Present the running application to the user for visual verification of all three new subagents. The app should already be running from Task 1.</action>
  <what-built>Three specialized subagents (Security Audit, MVC Analysis, Statistics Analysis) with parameterized launcher forms, Bedrock-powered tool-use execution, and structured markdown output in result cards.</what-built>
  <how-to-verify>
    1. Open the app (should be running from Task 1)
    2. Look at the sidebar — under "Subagents" section, verify you see:
       - Echo Test (Terminal icon) — existing
       - Security Audit (Shield icon) — NEW
       - MVC Analysis (Database icon) — NEW
       - Statistics Analysis (BarChart3 icon) — NEW
    3. Click "Security Audit" — verify an inline form appears with:
       - Label "AUDIT SCOPE *"
       - Placeholder text "Username, database name, or 'system-wide'"
       - Orange "Run Analysis" button
    4. Click "MVC Analysis" — verify form shows:
       - "DATABASE NAME *" field (required)
       - "TABLE NAME (OPTIONAL)" field
       - Orange "Run Analysis" button
    5. Click "Statistics Analysis" — verify same two-field form pattern
    6. (Optional, if Teradata + Bedrock are configured): Enter a real database name in MVC Analysis and click Run Analysis. Verify:
       - Spinner shows on the MVC card while running
       - Result appears as an orange-bordered card in the chat area
       - Result contains markdown with tables and SQL code blocks
    7. Verify Echo Test still works (click it — should run directly without form)
  </how-to-verify>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 4 subagents visible in sidebar
    - User confirms parameter forms render for parameterized subagents
    - User confirms Echo Test runs without form
  </acceptance_criteria>
  <done>User has verified all three subagents are visible, forms render, and the pipeline works end-to-end.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All 4 subagents visible in sidebar launcher
- Parameterized subagents show inline forms on click
- Non-parameterized subagents (Echo) run directly
- If tested with live connections, results render as structured markdown in result cards
</verification>

<success_criteria>
- User confirms all three new subagents are visible and launchable
- Parameter forms render correctly with inline styles
- At least one subagent produces output when run against a live environment (or user confirms they cannot test live but UI is correct)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-subagents/03-05-SUMMARY.md`
</output>
