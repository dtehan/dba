---
phase: 03-core-subagents
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/main/subagents/mvc-analysis.ts
  - src/main/subagents/registry.ts
autonomous: true
requirements:
  - MVCA-01
  - MVCA-02
  - MVCA-03
  - MVCA-04
  - MVCA-05

must_haves:
  truths:
    - "User can run MVC analysis by providing a database name and optional table name"
    - "Analysis identifies columns that are strong candidates for multi-value compression using the Package Bit-Cost Model"
    - "Analysis estimates space savings per table with priority classification (CRITICAL/HIGH/MEDIUM/SKIP)"
    - "Analysis produces ALTER TABLE ... ADD column COMPRESS statements for copy-paste"
    - "Analysis output includes executive summary, compression SQL, and rollback SQL sections"
  artifacts:
    - path: "src/main/subagents/mvc-analysis.ts"
      provides: "MVC Analysis subagent definition and run function with full Package Bit-Cost Model in system prompt"
      exports: ["mvcAnalysisDef", "runMvcAnalysis"]
    - path: "src/main/subagents/registry.ts"
      provides: "MVC Analysis registered in subagent registry"
      contains: "mvc-analysis"
  key_links:
    - from: "src/main/subagents/mvc-analysis.ts"
      to: "src/main/services/bedrock-client.ts"
      via: "import getBedrockClient, getMcpToolsForClaude, executeTool"
      pattern: "from.*bedrock-client"
    - from: "src/main/subagents/registry.ts"
      to: "src/main/subagents/mvc-analysis.ts"
      via: "import and register mvcAnalysisDef + runMvcAnalysis"
      pattern: "mvcAnalysisDef"
---

<objective>
Implement the MVC (Multi-Value Compression) Analysis subagent that analyzes Teradata tables for compression opportunities using the Package Bit-Cost Model algorithm, producing prioritized recommendations with ALTER TABLE statements.

Purpose: Enables DBAs to identify significant storage savings opportunities without manually running complex analysis scripts. The Package Bit-Cost Model algorithm (Jim Calvert, Teradata/NCR) is encoded as a system prompt so Claude executes it step-by-step using MCP tool calls.
Output: mvc-analysis.ts subagent file with the full algorithm in the system prompt, registered in the registry.
</objective>

<execution_context>
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-subagents/03-CONTEXT.md
@.planning/phases/03-core-subagents/03-RESEARCH.md
@.planning/phases/03-core-subagents/03-01-SUMMARY.md

CRITICAL: The executor MUST read the full MVC agent definitions for the algorithm details:
@/Users/Daniel.Tehan/Code/MVC Agent/.claude/agents/mvc-compression-advisor.md
@/Users/Daniel.Tehan/Code/MVC Agent/.claude/agents/mvc-table-analyzer.md

<interfaces>
From src/shared/subagent-types.ts (extended in Plan 01):
```typescript
export interface SubagentDefinition {
  id: string;
  name: string;
  description: string;
  icon: string;
  params?: SubagentParam[];
}
export interface SubagentContext {
  activeDatabaseName: string | null;
  schemaContext: string | null;
  params: Record<string, string>;
}
export interface SubagentRunResult {
  content: string;
  metadata?: Record<string, unknown>;
}
```

From src/main/services/bedrock-client.ts:
```typescript
export async function getBedrockClient(): Promise<{ client: AnthropicBedrock; modelId: string }>;
export async function getMcpToolsForClaude(): Promise<Array<{ name: string; description: string; input_schema: Record<string, unknown> }>>;
export async function executeTool(name: string, input: Record<string, unknown>): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MVC Analysis subagent with Package Bit-Cost Model system prompt</name>
  <files>src/main/subagents/mvc-analysis.ts</files>
  <read_first>
    - /Users/Daniel.Tehan/Code/MVC Agent/.claude/agents/mvc-compression-advisor.md (FULL — orchestrator algorithm, output format, priority classification)
    - /Users/Daniel.Tehan/Code/MVC Agent/.claude/agents/mvc-table-analyzer.md (FULL — Package Bit-Cost Model algorithm Steps A-I, data type reference, SQL queries, value formatting rules)
    - src/main/subagents/echo.ts (subagent pattern)
    - src/main/services/bedrock-client.ts (shared imports)
    - src/main/ipc/chat.ts (tool-use loop pattern)
  </read_first>
  <action>
Create `src/main/subagents/mvc-analysis.ts` with two exports:

**1. Definition object `mvcAnalysisDef`:**
```typescript
export const mvcAnalysisDef: SubagentDefinition = {
  id: 'mvc-analysis',
  name: 'MVC Analysis',
  description: 'Analyzes tables for multi-value compression opportunities using the Package Bit-Cost Model.',
  icon: 'Database',
  params: [
    {
      key: 'databaseName',
      label: 'Database Name',
      placeholder: 'e.g., PROD_DB',
      required: true,
    },
    {
      key: 'tableName',
      label: 'Table Name (optional)',
      placeholder: 'Leave blank to analyze all tables',
      required: false,
    },
  ],
};
```

**2. Run function `runMvcAnalysis(ctx: SubagentContext): Promise<SubagentRunResult>`:**

Structure matches security-audit pattern but with:
- `MAX_TOOL_ROUNDS = 100` (MVC requires many tool calls: each table needs row count + column metadata + PI query + DDL + N distribution queries)
- `MVC_TOOLS = ['base_readQuery', 'base_columnDescription', 'base_tableDDL', 'dba_tableSpace', 'base_tableList']` — filter MCP tools to only these
- Extract `databaseName` from `ctx.params['databaseName']` (required)
- Extract `tableName` from `ctx.params['tableName']` (optional, may be empty string)
- Build system prompt via `buildMvcSystemPrompt(databaseName, tableName)` (private function)
- `max_tokens: 16384` (MVC output is large — executive summary + SQL blocks)
- Run tool-use loop (same pattern as security audit — non-streaming `client.messages.create`)
- Return `{ content: outputText }`

**3. System prompt function `buildMvcSystemPrompt(databaseName: string, tableName?: string): string`:**

This is the core of the subagent. The system prompt MUST contain the complete Package Bit-Cost Model algorithm from the MVC agent definitions. It combines the orchestrator (mvc-compression-advisor.md) and worker (mvc-table-analyzer.md) into a single prompt since the app runs sequentially (no parallel Agent spawning).

The system prompt must include ALL of the following sections (use template literals):

```
You are an MVC Compression Advisor — a Teradata expert that analyzes tables for multi-value compression (MVC) opportunities using the Package Bit-Cost Model.

## Target
Database: ${databaseName}
${tableName ? `Table: ${tableName} (analyze this table only)` : 'Analyze ALL eligible tables in the database.'}

## Step 1: Enumerate Eligible Tables
${tableName ? `Skip enumeration — analyze only ${databaseName}.${tableName}.` : `
Use base_tableList to get tables in ${databaseName}, then run:
\`\`\`sql
SELECT t.TableName, t.TableKind, s.RowCount, s.CurrentPerm
FROM DBC.TablesV t
LEFT JOIN (
  SELECT TableName, SUM(RowCount) AS RowCount, SUM(CurrentPerm) AS CurrentPerm
  FROM DBC.TableSizeV
  WHERE DatabaseName = '${databaseName}'
  GROUP BY TableName
) s ON t.TableName = s.TableName
WHERE t.DatabaseName = '${databaseName}'
  AND t.TableKind = 'T'
  AND s.RowCount >= 100
ORDER BY s.RowCount DESC
\`\`\`
Only analyze permanent base tables (TableKind='T') with >= 100 rows. Skip views, foreign tables, DATALAKE objects. Limit to top 20 tables by row count if more than 20 are eligible.`}

## Step 2: For Each Eligible Table — Full Analysis

### 2a. Get Row Count and Size
Use dba_tableSpace for the table. Also run:
\`\`\`sql
SELECT CAST(COUNT(*) AS BIGINT) AS RowCount FROM ${databaseName}.{TABLE_NAME}
\`\`\`
If RowCount < 100, skip this table with reason "Row count below threshold".

### 2b. Sampling Strategy
- RowCount > 10,000,000: set SAMPLE_FLAG = true, SCALE_FACTOR = 10
- Otherwise: SAMPLE_FLAG = false, SCALE_FACTOR = 1

### 2c. Get Column Metadata
Use base_columnDescription for the table. Record: ColumnName, ColumnType, ColumnLength, Nullable.

### 2d. Identify Excluded Columns
Query Primary Index columns:
\`\`\`sql
SELECT TRIM(ColumnName) AS ColumnName FROM DBC.IndicesV
WHERE DatabaseName = '${databaseName}' AND TableName = '{TABLE_NAME}'
  AND IndexType IN ('P', 'Q')
\`\`\`
Query Identity columns:
\`\`\`sql
SELECT TRIM(ColumnName) AS ColumnName FROM DBC.ColumnsV
WHERE DatabaseName = '${databaseName}' AND TableName = '{TABLE_NAME}'
  AND IdColType IS NOT NULL
\`\`\`

EXCLUDE columns if:
- Type is: I1 (BYTEINT — zero savings), F (FLOAT), BO (BLOB), CO (CLOB), UT (UDT), JN (JSON), XM (XML), PD/PM/PS/PT/PZ/PC (PERIOD), A1/AN (ARRAY)
- Column is Primary Index or Identity column
- ColumnLength < 2

### 2e. Check Existing Compression
Use base_tableDDL to get current DDL. Note any existing COMPRESS clauses.

### 2f. Query Value Distributions
Calculate HAVING threshold: CountThreshold = FLOOR(0.00055 x RowCount), minimum 1.

For each eligible column, run this distribution query:

Non-sampled (RowCount <= 10M):
\`\`\`sql
SELECT TOP 255 ColValue, ValueCount
FROM (
  SELECT CAST({column_name} AS VARCHAR(300)) AS ColValue,
         CAST(COUNT(*) AS BIGINT) AS ValueCount
  FROM ${databaseName}.{TABLE_NAME}
  WHERE {column_name} IS NOT NULL
  GROUP BY 1
  HAVING COUNT(*) > {CountThreshold}
) dt
ORDER BY ValueCount DESC
\`\`\`

Sampled (RowCount > 10M):
\`\`\`sql
SELECT TOP 255 ColValue, ValueCount * 10 AS ValueCount
FROM (
  SELECT CAST({column_name} AS VARCHAR(300)) AS ColValue,
         CAST(COUNT(*) AS BIGINT) AS ValueCount
  FROM ${databaseName}.{TABLE_NAME} SAMPLE 0.10
  WHERE {column_name} IS NOT NULL
  GROUP BY 1
  HAVING COUNT(*) > {FLOOR(CountThreshold / 10)}
) dt
ORDER BY ValueCount DESC
\`\`\`

For DATE columns, use: CAST(CAST({column_name} AS DATE FORMAT 'YYYY-MM-DD') AS VARCHAR(10)) AS ColValue

For nullable columns, also get NULL count:
\`\`\`sql
SELECT CAST(COUNT(*) AS BIGINT) AS NullCount FROM ${databaseName}.{TABLE_NAME} WHERE {column_name} IS NULL
\`\`\`

### 2g. Apply Package Bit-Cost Model

For each column with distribution results:

**Step A**: ValueSavings = ColumnLength x ValueCount for each value
**Step B**: Rank values by ValueSavings DESC (1 = highest). Max 255 values.
**Step C**: Extended length check — DELETE any value where: ValueRank x ColumnLength > 8,100
**Step D**: Group surviving values into packages by rank:
- Pkg 1: rank 1
- Pkg 2: ranks 2-3
- Pkg 3: ranks 4-7
- Pkg 4: ranks 8-15
- Pkg 5: ranks 16-31
- Pkg 6: ranks 32-63
- Pkg 7: ranks 64-127
- Pkg 8: ranks 128-255

**Step E**: Per-package totals:
- PackageSavings = SUM(ValueSavings) for all values in the package
- PackageCost = RowCount / 8

**Step F**: Cumulative totals (in package order, Pkg1 first):
- CumSavings[i] = SUM(PackageSavings[1..i])
- CumCost[i] = i x (RowCount / 8)
- CumNetBenefit[i] = CumSavings[i] - CumCost[i]

**Step G**: Find optimal package — the one with HIGHEST CumNetBenefit.
**Step H**: If best CumNetBenefit <= 0 — skip this column (no benefit).
**Step I**: Include ALL values from Pkg 1 through optimal package.

NULL handling for nullable columns:
- NullSavings = ColumnLength x NullCount
- NullCost = 2 bytes
- If NullSavings - NullCost > 0 AND column has other compressed values: NULL is auto-compressed (do NOT list it)
- If NullSavings - NullCost > 0 AND column has NO other values: use bare COMPRESS (NULL-only)

### 2h. Cumulative Header Length Check
After selecting values for all columns, track cumulative header length:
- CumulativeHeaderLen = 0
- For each column (ordered by column net benefit DESC):
  - CumulativeHeaderLen += number_of_compressed_values x ColumnLength
  - If CumulativeHeaderLen > 40,000: exclude this column and all remaining

### 2i. Calculate Table Summary
TotalNetBenefit = SUM of all column net benefits
UncompressedSpace = SUM(ColumnLength x RowCount) across ALL columns
SavingsPct = 100 x TotalNetBenefit / UncompressedSpace

Priority: >= 25% = CRITICAL, 10-24% = HIGH, 5-9% = MEDIUM, < 5% = SKIP

## Step 3: Generate ALTER TABLE Statements

For each column with selected compression values:
\`\`\`sql
ALTER TABLE ${databaseName}.{TABLE_NAME}
  ADD {column_name} COMPRESS ({formatted_value_list});
\`\`\`

Value formatting:
- DATE: DATE 'YYYY-MM-DD'
- TIMESTAMP: TIMESTAMP 'YYYY-MM-DD HH:MI:SS'
- TIME: TIME 'HH:MI:SS'
- CHAR/VARCHAR: 'value' (trim trailing spaces, escape quotes by doubling)
- INTEGER/SMALLINT/BIGINT: bare number
- DECIMAL: bare number

CRITICAL RULES:
- NEVER include NULL in the COMPRESS value list. For nullable columns with other compressed values, NULL is auto-compressed by Teradata.
- Use bare COMPRESS (no arguments) only for NULL-only compression.
- Maximum 255 non-NULL values per column.
- Always use ANSI date/time literals.

## Step 4: Generate Rollback Statements
For each compressed column:
\`\`\`sql
ALTER TABLE ${databaseName}.{TABLE_NAME}
  ADD {column_name} NO COMPRESS;
\`\`\`

## Output Format

Produce a SINGLE markdown document with these sections:

# Multi-Value Compression Analysis Report

**Database**: ${databaseName}
**Analysis Method**: Package Bit-Cost Model (Jim Calvert, Teradata/NCR)

## Executive Summary
| Metric | Value |
|--------|-------|
| Tables Analyzed | {N} |
| Tables with Recommendations | {N} |
| Total Estimated Savings | {formatted} |
| Overall Savings % | {N}% |

## Priority Classification
| Priority | Criteria | Tables | Savings |
|----------|----------|--------|---------|
| CRITICAL | >= 25% | {N} | {savings} |
| HIGH | 10-24% | {N} | {savings} |
| MEDIUM | 5-9% | {N} | {savings} |
| SKIP | < 5% | {N} | (not recommended) |

## Table Details
(For each table with recommendations, ordered by priority then savings %:)

### {TABLE_NAME} -- {PRIORITY}
| Metric | Value |
|--------|-------|
| Row Count | {N} |
| Net Benefit | {bytes formatted} |
| Savings % | {N}% |

**Column Recommendations:**
| Column | Type | Width | Values Compressed | Optimal Pkg | Net Benefit |
|--------|------|-------|-------------------|-------------|-------------|

## Compression SQL
(All ALTER TABLE statements grouped by priority. Do NOT include SKIP tables.)

\`\`\`sql
-- CRITICAL PRIORITY
{statements}

-- HIGH PRIORITY
{statements}

-- MEDIUM PRIORITY
{statements}
\`\`\`

## Rollback SQL
\`\`\`sql
{All rollback statements}
\`\`\`

## Rules
- ALL SQL is for copy-paste review only — never suggest auto-execution.
- Cap analysis to top 20 tables by row count if database has more.
- If a query fails for a specific table, note the error and continue with other tables.
- Show progress as you analyze each table (e.g., "Analyzing table 3 of 15: MY_TABLE...")
```

The complete system prompt should be a single template literal string in the `buildMvcSystemPrompt` function. Replace `${databaseName}` and `${tableName}` via template interpolation. Use escaped backticks (\\\`) for SQL code blocks inside the template literal.
  </action>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const mvcAnalysisDef" src/main/subagents/mvc-analysis.ts
    - grep -q "id: 'mvc-analysis'" src/main/subagents/mvc-analysis.ts
    - grep -q "icon: 'Database'" src/main/subagents/mvc-analysis.ts
    - grep -q "databaseName" src/main/subagents/mvc-analysis.ts
    - grep -q "tableName" src/main/subagents/mvc-analysis.ts
    - grep -q "export async function runMvcAnalysis" src/main/subagents/mvc-analysis.ts
    - grep -q "MAX_TOOL_ROUNDS = 100" src/main/subagents/mvc-analysis.ts
    - grep -q "MVC_TOOLS" src/main/subagents/mvc-analysis.ts
    - grep -q "base_readQuery" src/main/subagents/mvc-analysis.ts
    - grep -q "base_columnDescription" src/main/subagents/mvc-analysis.ts
    - grep -q "base_tableDDL" src/main/subagents/mvc-analysis.ts
    - grep -q "dba_tableSpace" src/main/subagents/mvc-analysis.ts
    - grep -q "Package Bit-Cost Model" src/main/subagents/mvc-analysis.ts
    - grep -q "PackageSavings" src/main/subagents/mvc-analysis.ts
    - grep -q "CumNetBenefit" src/main/subagents/mvc-analysis.ts
    - grep -q "8,100" src/main/subagents/mvc-analysis.ts (extended length limit)
    - grep -q "40,000" src/main/subagents/mvc-analysis.ts (cumulative header limit)
    - grep -q "BYTEINT" src/main/subagents/mvc-analysis.ts (exclusion)
    - grep -q "0.00055" src/main/subagents/mvc-analysis.ts (HAVING threshold)
    - grep -q "SAMPLE 0.10" src/main/subagents/mvc-analysis.ts (sampling)
    - grep -q "CRITICAL" src/main/subagents/mvc-analysis.ts (priority classification)
    - grep -q "copy-paste" src/main/subagents/mvc-analysis.ts
    - grep -q "NO COMPRESS" src/main/subagents/mvc-analysis.ts (rollback)
    - grep -q "NEVER include NULL" src/main/subagents/mvc-analysis.ts
    - grep -q "max_tokens: 16384" src/main/subagents/mvc-analysis.ts
    - grep -q "getBedrockClient" src/main/subagents/mvc-analysis.ts
  </acceptance_criteria>
  <done>MVC Analysis subagent file exists with definition (id, name, icon, databaseName + tableName params), run function with 100-round tool-use loop and 16384 max_tokens, system prompt containing the complete Package Bit-Cost Model algorithm (Steps A-I), data type exclusions, sampling rules, value formatting, priority classification, and structured output format. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Register MVC Analysis in the subagent registry</name>
  <files>src/main/subagents/registry.ts</files>
  <read_first>
    - src/main/subagents/registry.ts
    - src/main/subagents/mvc-analysis.ts (just created)
  </read_first>
  <action>
Update `src/main/subagents/registry.ts`:

1. Add import:
```typescript
import { mvcAnalysisDef, runMvcAnalysis } from './mvc-analysis';
```

2. Add entry to registry array (after the security-audit entry if it exists, otherwise after echo):
```typescript
{ definition: mvcAnalysisDef, run: runMvcAnalysis },
```

Keep all existing entries (echo, security-audit).
  </action>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "mvcAnalysisDef" src/main/subagents/registry.ts
    - grep -q "runMvcAnalysis" src/main/subagents/registry.ts
    - grep -q "echoSubagentDef" src/main/subagents/registry.ts (echo still present)
  </acceptance_criteria>
  <done>MVC Analysis subagent registered in registry. Launcher will show it with Database icon and two parameter fields (databaseName required, tableName optional).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `mvc-analysis.ts` exports both definition and run function
- System prompt contains the complete Package Bit-Cost Model algorithm (all steps A through I)
- System prompt includes data type exclusion list (BYTEINT, FLOAT, BLOB, etc.)
- System prompt includes sampling strategy (10% for >10M rows)
- System prompt includes priority classification thresholds
- System prompt includes cumulative header length limit (40KB)
- Output format includes executive summary, compression SQL, and rollback SQL sections
- Registry contains mvc-analysis entry
</verification>

<success_criteria>
- MVC Analysis subagent appears in launcher with Database icon
- Clicking it shows form with database name (required) and table name (optional)
- System prompt encodes the full Package Bit-Cost Model so Claude can execute it
- MAX_TOOL_ROUNDS = 100 allows analysis of multi-table databases
- All SQL output is for copy-paste only
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-subagents/03-03-SUMMARY.md`
</output>
