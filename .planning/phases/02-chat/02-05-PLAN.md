---
phase: 02-chat
plan: 05
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-04]
files_modified: []
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, AGEN-01, AGEN-02, AGEN-03, UIBR-03]

must_haves:
  truths:
    - "User can type a message and receive a streaming Claude response"
    - "Chat is the default home screen with sidebar navigation"
    - "Subagent launcher visible in sidebar, echo subagent produces result card"
    - "Schema context loads when database is selected (if MCP available)"
    - "Markdown renders correctly in assistant responses"
---

<objective>
Visual verification checkpoint. Confirm the complete chat experience works end-to-end: streaming responses, markdown rendering, subagent launcher, and schema context injection.

Purpose: Human confirms the chat UI matches expectations before marking Phase 2 complete.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-chat/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of complete Phase 2 chat experience</name>
  <files>src/renderer/src/features/chat/ChatScreen.tsx, src/renderer/src/components/SubagentLauncher.tsx, src/renderer/src/components/Sidebar.tsx</files>
  <action>
Run `npm run dev` and verify the following:

1. Chat is default home screen. Sidebar shows Chat (active) at top, Settings below.
2. Type "What can you help me with?" and press Enter. Message appears right-aligned, response streams token-by-token.
3. Ask "Show me an example SELECT query for a Teradata employee table". SQL code block renders with highlighting and copy button.
4. Follow-up: "Make it join with the department table" — Claude remembers context.
5. Click Stop during streaming — it stops.
6. Shift+Enter creates newline, not send.
7. Sidebar has collapsible Subagents panel with Echo Test card.
8. Click Echo Test — orange-bordered result card appears in chat.
9. Navigate Chat/Settings — conversation preserved.
10. Schema context: if MCP available, verify schema-aware responses. If not, graceful degradation.
  </action>
  <verify>Human visual inspection of all 10 verification steps above.</verify>
  <done>User types "approved" confirming all Phase 2 success criteria are met, or provides an issue list for gap closure.</done>
  <read_first>
    - src/renderer/src/features/chat/ChatScreen.tsx
    - src/renderer/src/components/SubagentLauncher.tsx
    - src/renderer/src/components/Sidebar.tsx
  </read_first>
  <acceptance_criteria>
    - App launches with chat as default screen
    - Messages stream token-by-token
    - Markdown renders correctly (code blocks, tables, bold)
    - Subagent launcher visible and functional
    - Echo subagent produces orange-bordered result card
    - Navigation between Chat and Settings works
    - Stop button aborts streaming
    - Shift+Enter creates newline in input
  </acceptance_criteria>
</task>

</tasks>

<verification>
Human visual verification of all Phase 2 success criteria.
</verification>

<success_criteria>
User confirms the chat interface works as expected: streaming responses, rich markdown, subagent launcher, schema awareness, and proper navigation. All Phase 2 requirements visually verified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-chat/02-05-SUMMARY.md`
</output>
