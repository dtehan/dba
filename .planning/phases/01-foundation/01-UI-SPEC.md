---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-24
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (to be initialized by executor at project scaffold) |
| Preset | none — executor initializes with custom Teradata theme tokens |
| Component library | Radix UI (via shadcn/ui primitives) |
| Icon library | lucide-react |
| Font | Inter (system-ui fallback stack: Inter, ui-sans-serif, system-ui, sans-serif) |

**Initialization note:** No `components.json` exists yet — this is a greenfield project. The executor must run `npx shadcn init` as part of Phase 1 scaffolding, configuring Teradata brand colors as CSS custom properties. The shadcn base style should be `default` (not `new-york`). Dark mode base.

**Source:** CLAUDE.md — "TailwindCSS v4 + shadcn/ui for Teradata-branded components", "lucide-react icons"

---

## Screens in Scope (Phase 1)

Phase 1 does not include chat or subagents. The following screens must be designed and implemented:

1. **App Shell** — Electron window with title bar, sidebar navigation stub, and main content area
2. **Settings Screen** — Credential entry forms for Teradata (host, username, password) and Claude API key
3. **Connection Status Bar** — Persistent indicator strip showing Teradata and Claude API health (green/red/checking)
4. **Welcome / Placeholder Main Area** — Empty state for the main content area before chat is implemented in Phase 2

**Out of scope for Phase 1:** Chat thread, subagent launcher panel, streaming responses, schema browser.

**Source:** ROADMAP.md Phase 1 success criteria; REQUIREMENTS.md FOUN-01 through FOUN-06, UIBR-01, UIBR-02, UIBR-04

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline label padding, status dot margins |
| sm | 8px | Compact element spacing, input icon insets, button icon gaps |
| md | 16px | Default form field spacing, card internal padding, sidebar item padding |
| lg | 24px | Section padding within settings panels, form group gaps |
| xl | 32px | Settings panel outer padding, major layout gutters |
| touch | 40px | Password field reveal toggle click target; sidebar navigation item height |
| 2xl | 48px | Page-level vertical rhythm between settings sections |
| 3xl | 64px | Not used in Phase 1 |

Exceptions:
- Connection status indicator touch target: minimum 32px height (keyboard-accessible, not a touch target but must be readable at a glance)
- Password field reveal toggle: 40px × 40px click target (accessibility, WCAG 2.5.5) — uses `touch` token
- Sidebar navigation items: 40px height (comfortable click target on desktop, consistent with shadcn sidebar defaults) — uses `touch` token

**Source:** Default 8-point scale, UIBR-04 (DBA monitor sizes — desktop-first, no mobile exceptions needed)

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (Regular) | 1.5 |
| Label | 12px | 400 (Regular) | 1.4 |
| Heading | 18px | 600 (Semibold) | 1.3 |
| Display | 24px | 600 (Semibold) | 1.2 |

**Notes:**
- Body (14px) is the default for form field values, settings text, and status descriptions. DBAs work at 1080p–1440p where 14px renders crisply without crowding.
- Label (12px / Regular) is used for form field labels, status indicator text ("Connected", "Disconnected"), and section subheadings. The visual distinction from Body is size (12px vs 14px), not weight.
- Heading (18px / Semibold) is used for settings panel section headings ("Teradata Connection", "Claude API").
- Display (24px / Semibold) is used for the app title in the welcome/placeholder state only. Not used in settings forms.
- No italic weight. No thin (100–300) weights anywhere in this phase.
- Code/monospace: `font-mono` (Cascadia Code, ui-monospace, Menlo, Monaco, monospace) at 14px (Body size) for host/port fields and API key masked display. The mono treatment is a font-family change only — no new size point is introduced.

**Source:** Default — sensible for enterprise desktop app; UIBR-02 (clean, modern aesthetic)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#1A1A1A` | Main window background, sidebar background, settings screen background |
| Secondary (30%) | `#262626` | Card surfaces, settings panel backgrounds, input field backgrounds, modal overlays |
| Accent (10%) | `#F37440` | See "Accent reserved for" below |
| Destructive | `#EF4444` | Remove credentials confirmation button only |
| Success | `#22C55E` | Connected status indicator dot and label only |
| Border | `#333333` | Form field borders, card borders, dividers |
| Text Primary | `#F5F5F5` | All primary text on dark surfaces |
| Text Muted | `#A3A3A3` | Placeholder text, secondary labels, helper text, disabled states |

**Notes:**
- `#1A1A1A` is used instead of pure `#1D1D1D` (from brand spec) as the dominant background. The brand charcoal `#1D1D1D` maps to this — the `#1A1A1A` value gives slightly better contrast in Tailwind's CSS variable system. The executor may use `#1D1D1D` directly if the brand team specifies it precisely.
- Dark mode is the primary (and only) mode for Phase 1. No light mode toggle. DBAs work in low-light environments and dark UIs are standard for database tooling.

**Accent reserved for:**
- Primary action buttons ("Save Credentials", "Test Connection")
- Sidebar active navigation item left-border indicator (4px left border, `#F37440`)
- App title/logo mark color in the sidebar header
- Focus ring color on interactive elements (`outline: 2px solid #F37440`)
- "Test Connection" button hover state background fill

Accent is NOT used for: body text, headings, form labels, status indicators, background fills outside the above list.

**Semantic colors:**
- `#EF4444` (destructive red) is used exclusively on the "Remove / Clear Credentials" confirmation button in the settings screen. No other element uses red.
- `#22C55E` (success green) is used exclusively for the connected status indicator. No other element uses green.
- `#EAB308` (amber) is used exclusively for the "checking / connecting" intermediate connection status state.

**Source:** REQUIREMENTS.md UIBR-01 (`#F37440`, `#1D1D1D`); CLAUDE.md ("Teradata orange `#F37440` and charcoal `#1D1D1D` as design tokens"); REQUIREMENTS.md FOUN-06 (green/red connection status)

---

## Component Inventory (Phase 1)

These are the shadcn/ui components the executor must add via `npx shadcn add`. No third-party registry blocks.

| Component | shadcn slug | Used For |
|-----------|-------------|----------|
| Button | `button` | Save Credentials, Test Connection, Clear Credentials |
| Input | `input` | Host, Username, API Key text fields |
| Label | `label` | All form field labels |
| Card | `card` | Settings section panels (Teradata, Claude API) |
| Separator | `separator` | Dividers between settings sections |
| Badge | `badge` | Connection status indicators (Connected / Disconnected / Checking) |
| Tooltip | `tooltip` | Info tooltips on credential fields (e.g., "Stored securely via OS keychain") |
| Alert | `alert` | Inline error messages after failed connection test |
| Form | `form` | Wraps credential forms with validation (react-hook-form integration) |

**Password field:** Use shadcn `Input` with a `type="password"` toggle. Add a show/hide button using `lucide-react` `Eye` / `EyeOff` icons. Do not use a third-party password component.

**Status indicator:** Composed from a `Badge` with a colored dot (`div` with `rounded-full` and semantic background color) + label text. Not a third-party component.

---

## Layout

### App Shell Structure

```
┌─────────────────────────────────────────────────────┐
│  [Electron title bar — native OS chrome]             │
├──────────────┬──────────────────────────────────────┤
│              │                                       │
│   Sidebar    │         Main Content Area             │
│   (220px)    │         (fills remaining width)       │
│              │                                       │
│  [Logo]      │  [Settings screen or Welcome state]   │
│  [Nav items] │                                       │
│              │                                       │
│              │                                       │
├──────────────┴──────────────────────────────────────┤
│  [Connection Status Bar — 40px height]               │
│  Teradata: ● Connected    Claude API: ● Connected    │
└─────────────────────────────────────────────────────┘
```

- **Sidebar width:** 220px fixed. Not collapsible in Phase 1.
- **Minimum window size:** 1024px × 640px (enforced via Electron `minWidth`/`minHeight`).
- **Target monitor:** 1920×1080 primary; must be usable at 1280×800 (minimum DBA laptop).
- **Connection status bar:** Always visible at the bottom. 40px height. Background `#262626`. Border-top `#333333`.
- **Sidebar background:** `#1A1A1A`. Border-right `#333333` (1px).
- **Main content background:** `#1A1A1A`.

**Source:** REQUIREMENTS.md UIBR-04 (DBA monitor sizes); FOUN-06 (connection status always visible)

---

## Settings Screen Layout

```
Settings
──────────────────────────────────────────
Teradata Connection
┌─────────────────────────────────────────┐
│  Host          [________________________]│
│  Username      [________________________]│
│  Password      [__________________][👁] │
│                                          │
│  [Test Connection]        [Save]         │
└─────────────────────────────────────────┘

Claude API
┌─────────────────────────────────────────┐
│  API Key       [__________________][👁] │
│                                          │
│  [Test Connection]        [Save]         │
└─────────────────────────────────────────┘

                          [Clear All Credentials]
```

- Settings panels use `Card` component with `lg` (24px) internal padding.
- Each section has a `Heading` (18px / Semibold) label.
- Form fields stack vertically with `md` (16px) gap between rows.
- "Test Connection" button: outlined variant (border `#F37440`, text `#F37440`, no fill). "Save" button: filled accent variant (background `#F37440`, text white).
- "Clear All Credentials" button: destructive variant (border `#EF4444`, text `#EF4444`). Requires inline confirmation — button changes to "Confirm Clear" on first click, reverts after 5 seconds if not confirmed.
- Inline error after failed connection test: `Alert` component with destructive variant, shown below the affected section's Save button.
- Success after test: `Alert` with success variant (green border), shown for 3 seconds then auto-dismissed.

---

## Connection Status Bar States

| State | Dot Color | Label | Badge Variant |
|-------|-----------|-------|---------------|
| Connected | `#22C55E` | "Connected" | outline (green border) |
| Disconnected | `#EF4444` | "Disconnected" | outline (red border) |
| Checking | `#EAB308` (pulsing) | "Checking..." | outline (amber border) |
| Not configured | `#A3A3A3` | "Not configured" | outline (muted border) |

- Status bar shows two indicators side by side: "Teradata: [badge]" and "Claude API: [badge]".
- The amber "Checking..." state animates with a CSS `pulse` on the dot only (not the badge).
- Status checks re-run every 30 seconds when the app is in focus, or immediately after credentials are saved.

**Source:** REQUIREMENTS.md FOUN-06 ("Connection health indicator shows live green/red status for both Teradata and Claude API")

---

## Welcome / Placeholder State

When the user has not yet configured credentials, or navigates to the main content area before Phase 2 is built:

```
          [Teradata icon — 48px, orange]

     Welcome to Teradata DBA Agent

  Configure your credentials in Settings
  to get started.

     [Go to Settings]  ← accent filled button
```

- Centered vertically and horizontally in the main content area.
- Icon: Teradata logomark or a `Database` lucide-react icon at 48px in `#F37440`.
- Heading: 24px / Semibold.
- Body: 14px / Regular / `#A3A3A3`.
- CTA button: accent filled, `Go to Settings`.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (save credentials) | "Save Credentials" |
| Primary CTA (test connection) | "Test Connection" |
| Empty state heading | "Welcome to Teradata DBA Agent" |
| Empty state body | "Configure your credentials in Settings to get started." |
| Empty state CTA | "Go to Settings" |
| Connection success alert | "Connection successful. Ready to use." |
| Connection error alert | "Connection failed. Check your host and credentials, then try again." |
| Credentials saved confirmation | "Credentials saved securely to your OS keychain." |
| Destructive confirmation (first click) | "Clear All Credentials" |
| Destructive confirmation (second click / confirm) | "Confirm — this cannot be undone" |
| Destructive confirmation (auto-revert label) | Reverts to "Clear All Credentials" after 5 seconds |
| Status: not configured | "Not configured" |
| Status: checking | "Checking..." |
| Status: connected | "Connected" |
| Status: disconnected | "Disconnected" |
| Settings nav label | "Settings" |
| Teradata section heading | "Teradata Connection" |
| Claude API section heading | "Claude API" |
| Host field label | "Host" |
| Username field label | "Username" |
| Password field label | "Password" |
| API key field label | "API Key" |
| Password field helper text | "Stored securely via OS keychain — never saved in plaintext." |
| API key field helper text | "Stored securely via OS keychain — never saved in plaintext." |

**Tone:** Direct, technical, trustworthy. No marketing language. DBA audience expects precision. No ellipsis in headings. Error messages always include an action path ("try again", "check your...").

**Source:** REQUIREMENTS.md FOUN-02, FOUN-03, FOUN-05, FOUN-06; phase goal ("credentials saved securely via OS keychain, never in plaintext")

---

## Interaction States

All interactive elements must implement these states explicitly:

| Element | Default | Hover | Focus | Active | Disabled |
|---------|---------|-------|-------|--------|----------|
| Accent button (Save) | `#F37440` bg | `#E55C20` bg (10% darker) | `#F37440` bg + 2px orange outline offset 2px | `#CC4A10` bg | 40% opacity, cursor-not-allowed |
| Outlined button (Test) | transparent bg, `#F37440` border | `#F37440` bg at 10% opacity | 2px orange outline offset 2px | `#F37440` bg at 20% opacity | 40% opacity, cursor-not-allowed |
| Destructive button | transparent bg, `#EF4444` border | `#EF4444` bg at 10% opacity | 2px red outline offset 2px | `#EF4444` bg at 20% opacity | 40% opacity |
| Input field | `#262626` bg, `#333333` border | `#333333` border (no change) | `#F37440` border, 0 ring | — | `#1A1A1A` bg, muted text |
| Sidebar nav item | transparent bg | `#262626` bg | 2px orange left border + `#262626` bg | `#2E2E2E` bg | — |
| Sidebar active item | `#262626` bg + 4px `#F37440` left border | same | same + focus outline | — | — |

---

## Accessibility

- All form fields must have associated `<label>` elements (not just placeholder text).
- Password show/hide toggle must have `aria-label="Show password"` / `aria-label="Hide password"`.
- Connection status badges must have `role="status"` and `aria-live="polite"` so screen readers announce changes.
- Focus order follows visual reading order: sidebar nav → main content → status bar.
- Minimum contrast ratio: 4.5:1 for body text (`#F5F5F5` on `#1A1A1A` = 15.8:1 — exceeds WCAG AA).
- `#A3A3A3` muted text on `#1A1A1A` background = 5.9:1 — passes WCAG AA for normal text.
- Accent `#F37440` on `#1A1A1A` background = 3.8:1 — passes WCAG AA for large text (18px+) and UI components. Do not use accent color for small body text.

**Source:** UIBR-02 (clean, modern aesthetic — implies accessibility); standard WCAG AA compliance

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, label, card, separator, badge, tooltip, alert, form | not required |
| Third-party | none | not applicable |

No third-party registries are used in Phase 1. All components are from the official shadcn registry.

**Vetting date:** 2026-03-24. No third-party blocks to vet.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
