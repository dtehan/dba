---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - src/renderer/src/features/settings/SettingsScreen.tsx
  - src/renderer/src/features/settings/TeradataForm.tsx
  - src/renderer/src/features/settings/ClaudeApiForm.tsx
  - src/renderer/src/components/AppShell.tsx
autonomous: false
requirements:
  - FOUN-02
  - FOUN-06
must_haves:
  truths:
    - "User can enter Teradata host, username, and password in a form"
    - "User can enter Claude API key in a form"
    - "Save Credentials button encrypts and persists credentials via IPC"
    - "Test Connection button validates connectivity and shows success/error inline"
    - "Clear All Credentials button requires confirmation before clearing"
    - "Password and API key fields have show/hide toggle"
    - "Form validation errors show inline for empty required fields"
    - "Status bar updates after credentials are saved and tested"
  artifacts:
    - path: "src/renderer/src/features/settings/SettingsScreen.tsx"
      provides: "Settings page with Teradata and Claude API sections"
      contains: "Settings"
    - path: "src/renderer/src/features/settings/TeradataForm.tsx"
      provides: "Teradata credential form with host, username, password"
      contains: "Host"
    - path: "src/renderer/src/features/settings/ClaudeApiForm.tsx"
      provides: "Claude API key form"
      contains: "API Key"
  key_links:
    - from: "src/renderer/src/features/settings/TeradataForm.tsx"
      to: "src/renderer/src/lib/ipc.ts"
      via: "getElectronAPI().saveTeradataCredentials()"
      pattern: "saveTeradataCredentials"
    - from: "src/renderer/src/features/settings/ClaudeApiForm.tsx"
      to: "src/renderer/src/lib/ipc.ts"
      via: "getElectronAPI().saveClaudeApiKey()"
      pattern: "saveClaudeApiKey"
    - from: "src/renderer/src/features/settings/TeradataForm.tsx"
      to: "src/renderer/src/lib/ipc.ts"
      via: "getElectronAPI().testTeradataConnection()"
      pattern: "testTeradataConnection"
    - from: "src/renderer/src/components/AppShell.tsx"
      to: "src/renderer/src/features/settings/SettingsScreen.tsx"
      via: "Conditional render when currentPage === 'settings'"
      pattern: "SettingsScreen"
---

<objective>
Build the Settings screen with Teradata credential form, Claude API key form, connection testing, and credential clearing -- all wired to the IPC backend from Plans 02/03.

Purpose: This is the primary user interaction for Phase 1 -- where DBAs enter their credentials and verify connectivity before any AI features are available.
Output: Fully functional Settings screen matching UI-SPEC with save, test, clear, and inline feedback.
</objective>

<execution_context>
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/Code/dba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md

<interfaces>
<!-- From Plan 02 -- IPC API available to renderer -->
From src/renderer/src/lib/ipc.ts:
```typescript
export function getElectronAPI(): ElectronAPI;
```

From src/shared/types.ts:
```typescript
export interface ElectronAPI {
  saveTeradataCredentials: (creds: TeradataCredentials) => Promise<void>;
  loadTeradataHost: () => Promise<string | null>;
  saveClaudeApiKey: (key: string) => Promise<void>;
  clearAllCredentials: () => Promise<void>;
  hasTeradataCredentials: () => Promise<boolean>;
  hasClaudeKey: () => Promise<boolean>;
  testTeradataConnection: () => Promise<{ success: boolean; error?: string }>;
  testClaudeConnection: () => Promise<{ success: boolean; error?: string }>;
}
```

From src/shared/schemas.ts:
```typescript
export const teradataCredentialsSchema = z.object({
  host: z.string().min(1, 'Host is required'),
  username: z.string().min(1, 'Username is required'),
  password: z.string().min(1, 'Password is required'),
});
export const claudeApiKeySchema = z.string().min(1).startsWith('sk-');
```

<!-- From Plan 04 -- Zustand store and navigation -->
From src/renderer/src/store/app-store.ts:
```typescript
export const useAppStore = create<AppState>(...);
// currentPage: NavPage ('welcome' | 'settings')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TeradataForm and ClaudeApiForm with save, test, and validation</name>
  <files>src/renderer/src/features/settings/TeradataForm.tsx, src/renderer/src/features/settings/ClaudeApiForm.tsx</files>
  <read_first>
    - /Users/Daniel.Tehan/Code/dba/.planning/phases/01-foundation/01-UI-SPEC.md (Settings Screen Layout, Interaction States, Copywriting Contract, Accessibility)
    - /Users/Daniel.Tehan/Code/dba/src/shared/schemas.ts (Zod validation schemas)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/lib/ipc.ts (getElectronAPI)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/button.tsx (button variants)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/input.tsx (input component)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/card.tsx (card component)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/form.tsx (form component with react-hook-form)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/alert.tsx (alert for success/error)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/label.tsx (label component)
  </read_first>
  <action>
1. Create `src/renderer/src/features/settings/TeradataForm.tsx`:
   - Use `react-hook-form` with `@hookform/resolvers/zod` and `teradataCredentialsSchema`.
   - Wrap in shadcn `Card` with `lg` (24px) internal padding (`p-6`).
   - Section heading: "Teradata Connection" at 18px / Semibold -- use `text-lg font-semibold text-text-primary`.
   - Fields stack vertically with `md` (16px) gap -- `space-y-4`:
     - **Host** field: shadcn `Input`, `Label` text "Host", font-mono for the input value. Use `FormField`, `FormItem`, `FormLabel`, `FormControl`, `FormMessage` from shadcn form.
     - **Username** field: shadcn `Input`, `Label` text "Username".
     - **Password** field: shadcn `Input` with `type` toggled between "password" and "text". Add a show/hide button using `Eye`/`EyeOff` from lucide-react. The toggle button:
       - 48x48px click target (per UI-SPEC 2xl spacing)
       - `aria-label="Show password"` / `aria-label="Hide password"` (per UI-SPEC Accessibility)
       - Positioned inside the input using `relative` parent + `absolute right-0` button
     - Helper text below password: "Stored securely via OS keychain -- never saved in plaintext." in 12px `text-text-muted`.
   - Load existing host on mount: call `getElectronAPI().loadTeradataHost()` in `useEffect` and pre-fill the host field.
   - Buttons row at the bottom of the card:
     - **"Test Connection"** button: outlined variant -- `variant="outline"` with custom Teradata orange border. `border-td-orange text-td-orange hover:bg-td-orange/10 focus-visible:outline-2 focus-visible:outline-td-orange focus-visible:outline-offset-2`. On click: call `getElectronAPI().testTeradataConnection()` with a loading state. Disabled while testing.
     - **"Save Credentials"** button: accent filled -- custom `bg-td-orange text-white hover:bg-td-orange-hover active:bg-td-orange-active focus-visible:outline-2 focus-visible:outline-td-orange focus-visible:outline-offset-2`. On click: validate form, then call `getElectronAPI().saveTeradataCredentials({ host, username, password })`. Disabled while saving.
   - Feedback states:
     - Test success: Show shadcn `Alert` with success styling (green border) below buttons. Text: "Connection successful. Ready to use." Auto-dismiss after 3 seconds.
     - Test failure: Show `Alert` with destructive variant. Text: "Connection failed. Check your host and credentials, then try again." Persists until next action.
     - Save success: Show `Alert` with success variant. Text: "Credentials saved securely to your OS keychain." Auto-dismiss after 3 seconds.
   - Both buttons disabled (40% opacity, cursor-not-allowed) when form is invalid or an operation is in progress.

2. Create `src/renderer/src/features/settings/ClaudeApiForm.tsx`:
   - Same card structure as TeradataForm.
   - Section heading: "Claude API" at 18px / Semibold.
   - Single field: **API Key** with show/hide toggle (same pattern as password field). `aria-label="Show API key"` / `aria-label="Hide API key"`.
   - Helper text: "Stored securely via OS keychain -- never saved in plaintext."
   - Use a simple `useState` + manual validation (or react-hook-form with `claudeApiKeySchema` wrapped in a z.object).
   - Validation: key must start with "sk-" and be non-empty. Show form error if invalid.
   - Same two buttons: "Test Connection" (outlined) and "Save Credentials" (accent filled).
   - Test calls `getElectronAPI().testClaudeConnection()`.
   - Save calls `getElectronAPI().saveClaudeApiKey(key)`.
   - Same success/error alert pattern as TeradataForm.
  </action>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Teradata Connection"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Host"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Username"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Password"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Test Connection"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Save Credentials"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Stored securely via OS keychain"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Connection successful"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Connection failed"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "Eye"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "aria-label"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "saveTeradataCredentials"
    - src/renderer/src/features/settings/TeradataForm.tsx contains "testTeradataConnection"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "Claude API"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "API Key"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "Save Credentials"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "Test Connection"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "saveClaudeApiKey"
    - src/renderer/src/features/settings/ClaudeApiForm.tsx contains "testClaudeConnection"
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Both credential forms implemented with validation, save/test buttons, password visibility toggle, inline success/error alerts, proper disabled states, and accessibility attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Build SettingsScreen and wire into AppShell navigation</name>
  <files>src/renderer/src/features/settings/SettingsScreen.tsx, src/renderer/src/components/AppShell.tsx</files>
  <read_first>
    - /Users/Daniel.Tehan/Code/dba/.planning/phases/01-foundation/01-UI-SPEC.md (Settings Screen Layout, Copywriting Contract for "Clear All Credentials")
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/features/settings/TeradataForm.tsx (already created)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/features/settings/ClaudeApiForm.tsx (already created)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/AppShell.tsx (needs settings route)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/button.tsx (destructive variant)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/components/ui/separator.tsx (divider between sections)
    - /Users/Daniel.Tehan/Code/dba/src/renderer/src/lib/ipc.ts (for clearAllCredentials)
  </read_first>
  <action>
1. Create `src/renderer/src/features/settings/SettingsScreen.tsx`:
   - Page heading: "Settings" at 24px / Semibold (`text-2xl font-semibold`) with `xl` (32px) padding.
   - Scrollable content area with max-width for readability (e.g., `max-w-2xl`).
   - Stack: `<TeradataForm />`, `<Separator />`, `<ClaudeApiForm />`, `<Separator />`, Clear button area.
   - Vertical gap between sections: `lg` (24px) -- `space-y-6`.
   - **"Clear All Credentials"** button at the bottom, right-aligned:
     - Destructive outlined style: `border-destructive text-destructive hover:bg-destructive/10`
     - Implements inline confirmation per UI-SPEC:
       - First click: button text changes to "Confirm -- this cannot be undone"
       - Second click within 5 seconds: calls `getElectronAPI().clearAllCredentials()`
       - If no second click within 5 seconds: reverts button text to "Clear All Credentials"
     - Use `useState` for confirmation state and `useEffect` with `setTimeout(5000)` for auto-revert.
     - After clearing, optionally reset both forms (or reload the page state).

2. Update `src/renderer/src/components/AppShell.tsx`:
   - Import `SettingsScreen` from '../features/settings/SettingsScreen'.
   - Replace the placeholder settings div with `<SettingsScreen />` when `currentPage === 'settings'`.
   - The conditional render should be:
     ```typescript
     const currentPage = useAppStore((s) => s.currentPage);
     // In JSX main area:
     {currentPage === 'welcome' && <WelcomeState />}
     {currentPage === 'settings' && <SettingsScreen />}
     ```
  </action>
  <verify>
    <automated>cd /Users/Daniel.Tehan/Code/dba && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "Settings"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "TeradataForm"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "ClaudeApiForm"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "Clear All Credentials"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "Confirm"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "this cannot be undone"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "clearAllCredentials"
    - src/renderer/src/features/settings/SettingsScreen.tsx contains "5000" or "5_000" (timeout for confirmation revert)
    - src/renderer/src/components/AppShell.tsx contains "SettingsScreen"
    - src/renderer/src/components/AppShell.tsx contains "currentPage === 'settings'" or equivalent conditional
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>SettingsScreen assembles both credential forms with clear-all confirmation. AppShell routes to SettingsScreen when Settings nav is active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1 UI</name>
  <files>none</files>
  <action>
    Human visually verifies the complete Phase 1 application by running `npm run dev` and checking all screens, interactions, and Teradata branding against the UI-SPEC.
  </action>
  <what-built>
    Complete Phase 1 application: Electron desktop app with Teradata-branded dark UI, sidebar navigation, settings screen with credential forms (Teradata host/username/password + Claude API key), connection test buttons, status bar with health indicators, and welcome state.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/Daniel.Tehan/Code/dba && npm run dev`
    2. Electron window should open at minimum 1024x640
    3. Verify welcome state: centered Database icon (orange), "Welcome to Teradata DBA Agent" heading, "Go to Settings" button
    4. Click "Go to Settings" or the Settings sidebar nav item
    5. Verify settings screen:
       - "Teradata Connection" card with Host, Username, Password fields
       - "Claude API" card with API Key field
       - Password and API key fields have show/hide eye toggle
       - "Test Connection" (outlined orange border) and "Save Credentials" (filled orange) buttons on each section
       - "Clear All Credentials" button at bottom (red outlined)
    6. Verify status bar at bottom: "Teradata: Not configured" and "Claude API: Not configured" badges
    7. Verify sidebar: 220px width, Settings item with orange left border when active
    8. Verify colors: dark background (#1A1A1A), card surfaces (#262626), orange accent (#F37440)
    9. Try entering credentials and clicking "Save Credentials" -- should show "Credentials saved securely to your OS keychain" alert
    10. Click "Clear All Credentials" -- should change to "Confirm -- this cannot be undone", revert after 5 seconds
  </how-to-verify>
  <verify>Human visual inspection confirms UI matches UI-SPEC</verify>
  <done>User has approved the visual appearance and interaction flow of the complete Phase 1 UI.</done>
  <resume-signal>Type "approved" to confirm UI matches expectations, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm run dev` launches the Electron app with complete Phase 1 UI
- Settings screen has all fields per UI-SPEC
- Status bar shows correct connection states
- Credential save/test/clear flow works end-to-end
- All UI elements use Teradata design tokens
</verification>

<success_criteria>
- Settings screen matches UI-SPEC layout: two card sections stacked vertically
- All form fields have labels, validation, and proper placeholder text
- Password/API key fields have functional show/hide toggle with correct aria-labels
- Test Connection calls IPC and shows inline success/error alerts
- Save Credentials persists via safeStorage and shows confirmation
- Clear All Credentials has 5-second confirmation flow
- Status bar updates reflect credential state
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
